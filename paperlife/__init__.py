"""PaperLife — printable money-life PDF kit generator.

Three products, one shared renderer:

* ICE / end-of-life binder (8+ pages, one page per thing)
* Cash-stuffing allocator kit (exact allocation math + labels + trackers)
* Net-worth certificate (one page, honest math)

Every page carries a compliance footer; the PNG previews are rendered from
the same page objects as the PDFs (no mockups, no stock).
"""

__version__ = "0.1.0"

PRODUCTS = ("ice", "cash", "networth")
